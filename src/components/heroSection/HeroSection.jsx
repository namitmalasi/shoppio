import hero from "../../img/hero.png";
const HeroSection = () => {
  return (
    <div>
      <img src={hero} alt="hero image" className="w-full h-40 lg:h-full" />
    </div>
  );
};

export default HeroSection;
